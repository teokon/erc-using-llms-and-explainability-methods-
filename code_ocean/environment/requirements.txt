# Minimal, CPU-only dependencies for the Code Ocean figure/table capsule.
# (The capsule only runs the artifact-based post-processing scripts, so it needs neither
#  torch/transformers nor a GPU. The full pipeline's deps are in the repo-root requirements.txt.)
numpy>=1.26,<2.0
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
python-docx>=1.1
